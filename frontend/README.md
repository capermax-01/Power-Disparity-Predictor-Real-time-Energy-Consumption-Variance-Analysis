# Frontend — Power Disparity Predictor

This folder contains a Vite + React + TypeScript frontend for the Power Disparity Predictor.

Quick start:

```bash
cd frontend
npm install
npm run dev
```

Configure API endpoint in `.env.local`:

```
VITE_API_URL=http://localhost:8000
```
